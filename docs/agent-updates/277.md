        # Docs update for #277

        Reconcile CRM schema documentation with canonical migrations 015 and 016

        ## Requested

        ## Summary

Repair CRM schema documentation that regressed after the migration-013 reconciliation work.

At audited revision c86440ea, docs/CRM_SCHEMA.md describes legacy company pipeline columns such as owner, expected_value, and stage_reason. The canonical schema uses pipeline_owner, expected_value_cents, pipeline_loss_reason, and pipeline_nurture_reason. The migration inventory also stops at 014 and omits migration 015's reconciliation and migration 016's payment details.

PR #218 updated this documentation, but the integrated main branch no longer contains the canonical version.

Related: #210, #230, PR #218.

## Required updates

- Document the canonical companies table exactly as created after all current migrations.
- Replace legacy pipeline names with:
  - pipeline_owner
  - expected_value_cents
  - pipeline_loss_reason
  - pipeline_nurture_reason
- Describe monetary units, nullability, defaults, constraints, and indexes accurately.
- Add migrations 015 and 016 to the migration ledger with their purpose, compatibility behavior, and rollback posture.
- Explain how migration 015 reconciles legacy migration-013 deployments without rewriting applied migration history.
- Clearly mark retained legacy columns/tables, if any, as compatibility artifacts rather than canonical application fields.
- Verify contact, activity, pipeline, brief-conversion, and payment sections against the live migration definitions and repository SQL.
- Repair links or runbooks that still refer to the legacy names.

## Acceptance criteria

- [x] Every documented CRM column name matches the post-016 schema
- [x] Types, nullability, defaults, constraints, and indexes are accurate
- [x] Legacy columns are removed from canonical tables or explicitly labeled as compatibility-only
- [x] Migration entries cover 001 through 016 without gaps
- [x] Migration 015 documents reconciliation, backfill, idempotency, and frozen-digest behavior
- [x] Migration 016 documents project-brief payment detail fields
- [x] Repository and service terminology matches the documentation
- [x] No operational query or example recommends a legacy pipeline column
- [x] A lightweight documentation/schema drift check prevents the same regression
- [x] The change is ready to deploy in the PR

## Required validation

- Build a real PostgreSQL database from migrations 001–016 and compare the resulting schema to the document.
- Exercise the legacy-013 upgrade path before validating the reconciled schema.
- Search documentation and runbooks for stale legacy identifiers.
- Add a deterministic check for the canonical pipeline-column set and migration ledger.

## Non-goals

- Do not rename canonical database columns merely to match stale documentation.
- Do not mutate historical migration files or stored migration digests.

        ## Status

        Docs agent created this page as a visible PR artifact. Expand or
        replace with the authoritative documentation for the change.
