        # Builder worklog for #15

        Title: Add hello-world HTTP API with tests and deploy: Deploy config is in-repo (e.g. `render.yaml` or documented one-click) and servic

        ## Acceptance

        Child of #11 (one-commit unit).

## Scope

Deploy config is in-repo (e.g. `render.yaml` or documented one-click) and service is deployable

Parent: #11

        ## Notes

        Autonomous code generation is not wired in this runner yet.
        This commit/PR is the visible handoff artifact for review.
